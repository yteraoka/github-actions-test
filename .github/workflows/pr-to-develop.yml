name: My Workflow Name

run-name: "develop branch への変更に対する workflow for ${{ github.workflow }} by @${{ github.actor }}"

on:
  workflow_dispatch:

  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - develop

  push:
    branches:
      - develop

jobs:
  test:
    name: test 1
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: test step 1
        run: |
          echo "step 1"
